dataset:
  num_steps: ${num_frames}
  frameskip: 5
  name: pusht_expert_train.lance
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state
